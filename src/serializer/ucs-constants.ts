export const UCS_APOSTROPHE = new Uint8Array([0x27]); // `'`
export const UCS_OPENING_PARENTHESIS = new Uint8Array([0x28]); // `(`
export const UCS_CLOSING_PARENTHESIS = new Uint8Array([0x29]); // `)`
export const UCS_LIST_ITEM_SEPARATOR = new Uint8Array([0x29, 0x28]); // `)(`
export const UCS_EMPTY_LIST = new Uint8Array([0x21, 0x21]); // `!!`
export const UCS_EMPTY_MAP = new Uint8Array([0x24]); // `$`
export const UCS_NULL = new Uint8Array([0x2d, 0x2d]); // `--`
export const UCS_NULL_ENTRY_VALUE = new Uint8Array([0x2d, 0x2d, 0x29]); // `--)`
